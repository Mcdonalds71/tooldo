node_modules/
dist/
.astro/
.wrangler/

# The Fontshare EULA permits using and self-hosting Clash Display but not
# redistributing the files, and a public repo is redistribution. Fetch it per
# checkout — see public/fonts/README.md.
public/fonts/clash-display-variable.woff2

# onnxruntime-web's WASM binaries, copied from node_modules by
# scripts/copy-onnx-runtime.mjs (postinstall) so the Background Remover never fetches
# them from a third-party CDN. Derived from an already-licensed dependency, not source.
public/ort/

# test output
test-results/
playwright-report/
coverage/

# local env + editor noise
.env
.env.*
!.env.example
*.local
.DS_Store
Thumbs.db
