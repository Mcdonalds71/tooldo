throw new Error(
  'onnxruntime-node is stubbed out in this repo — tooldo is browser-only and never runs ' +
    "this package's real code. If something imported it, that code path only belongs in a " +
    'Worker/browser bundle, which should be resolving onnxruntime-web instead.',
);
